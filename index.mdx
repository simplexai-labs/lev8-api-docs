---
title: "lev8 API"
description: "Search for entities and contact details from your application."
---

The lev8 API turns a natural-language objective into matching entities or a requested contact detail. It is designed for server-side products that need people and business intelligence without building a search pipeline from scratch.

## Start with Entity Search

Create an Entity Search task, poll its status, then fetch results by position.

```bash
curl "https://app.lev8.com/v1/entity-search/create_task" \
  --request POST \
  --header "Content-Type: application/json" \
  --header "x-api-key: $LEV8_API_KEY" \
  --data '{
    "entity_type": "company",
    "objective": "AI infrastructure startups in the United States founded since 2023",
    "limit": 20
  }'
```

The response includes a `leads_search_id`. Use it with `GET /v1/entity-search/status` to check progress and `GET /v1/entity-search/fetch` to read available results.

<CardGroup cols={2}>
  <Card title="Quickstart" icon="rocket" href="/quickstart">
    Create a task, check status, and fetch the first result batch.
  </Card>
  <Card title="Authentication" icon="key" href="/authentication">
    Create, store, and rotate an API key safely.
  </Card>
  <Card title="Entity Search" icon="magnifying-glass" href="/api-reference/entity-search">
    Review task fields, entity types, fetch parameters, and response objects.
  </Card>
  <Card title="Contact Search" icon="address-book" href="/api-reference/contact-search">
    Find an email address or phone number with a JSON request.
  </Card>
  <Card title="Task results" icon="list-checks" href="/api-reference/task-results">
    Read results with `pts`, `num`, `count`, and `ready_count`.
  </Card>
  <Card title="Errors" icon="triangle-exclamation" href="/api-reference/errors">
    Handle HTTP errors and task-level failures.
  </Card>
</CardGroup>

## API behavior at a glance

- Authentication uses an `x-api-key` header. API keys are separate from user sessions and internal service credentials.
- Entity Search supports company, person, creator, and Shopify store entity types.
- `POST /v1/entity-search/create_task` returns a `leads_search_id` for later status checks and result fetches.
- `GET /v1/entity-search/fetch` reads results from `pts` and returns `count`; compute the next `pts` as `pts + count`.
- A task is fully consumed when the task status is `done` and your client has read every result up to `ready_count`.
- `POST /v1/contact-search` returns one email or phone lookup as JSON after billing settles.
- `GET /v1/credit/balance` reports the credits currently available for new requests.
- Entity Search reserves credits up to the requested `limit` and settles against the number of unique returned entities. Contact Search reserves one lookup at the configured contact-type price.

<Warning>
  Credit history and bucket-detail endpoints, response replay, and client-controlled API scopes are not part of the current public API.
</Warning>
